version: '3.4'

services:
  server:
    image: server
    build:
      context: ./server
      dockerfile: ../.devcontainer/server/Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
    volumes:
      - ./server:/workspace
      #- ~/.gitconfig:/root/.gitconfig
    #env_file:
      #- ./.devcontainer/server/devcontainer.env